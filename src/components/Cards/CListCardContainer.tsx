import React from "react";
import BaseContainer from "../Container/BaseContainer";

const CListCardContainer = (props) => {
  return <BaseContainer>{props.children}</BaseContainer>;
};

export default CListCardContainer;
